/*

  use cases covered:
  
  * batch queue (batchdb)
  * batch queue for shell scripts
  * batch queue for shell scripts with http api
  * batch queue for shell scripts with http ui
  * batch queue for shell scripts with http api and ui
  * batch queue scripts with http api
  * batch queue scripts with http ui
  * batch queue scripts with http api and http ui
  * shell command capture and parsing

  packages written:
  
  * batchdb
  * batchdb-shell
  * shell-pack
  * batchdb-web-api
  * batchdb-web-ui

*/
